# Use ``cat'' as a phony ``make'' to see output and only run ``build'' itself

fixture hello

build_or_fail -s

echo "[Makefile]"
MAKE=cat build_or_fail

echo
echo "[Makefile dependencies]"
for f in target/dep/*; do
  ar p $f
done
